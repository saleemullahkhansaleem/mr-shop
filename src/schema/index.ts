import { productSchema } from "./productScema";
import { categorySchema } from "./categorySchema";
import { reviewSchema } from "./reviewSchema";

export { productSchema, categorySchema, reviewSchema };
